import fetch from 'node-fetch';

const CHERY_DMS_URL = "http://localhost:5173/api/chery_dms"; // wait, the dev server is running on localhost:5173.
// But we can query the backend direct URL if we look at api/chery_dms.js!
// Let's find the direct URL by checking api/chery_dms.js.
